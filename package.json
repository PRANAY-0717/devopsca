{
  "name": "ca2-server",
  "version": "1.0.0",
  "description": "Server with health check endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Running tests...\" && exit 0"
  },
  "dependencies": {
    "express": "^4.22.1"
  }
}
